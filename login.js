// LOGIN USER

const isLoggedIn = false;

let usrAttempts = 3;

const usr = document.querySelector('email').textContent;
const passWord = document.querySelector('password');

if (usr in usrAccounts && password in usrAccounts)
